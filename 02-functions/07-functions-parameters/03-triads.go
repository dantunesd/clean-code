package main

/**
 * Triads functions are functions that receive three parameters. It's starts to get harder to understand its parameters
 * If you can simplify it to a monadic or dyatic function, do it.
 */

// Receiving three params
func MakeCircle(x, y, radius float64) string {
	return "creating a circle with the three params"
}
