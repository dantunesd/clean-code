package main

// You can name things using "domain solution" names. Other programmers certanly will know what it means (Ex: Strategy Pattern)
type BusStrategy struct{}

// You can name things using "domain solution" names. Other programmers certanly will know what it means (Ex: State Pattern)
type ActiveAccountState struct{}

// Or you can just use "domain problem" names if there aren't any "domain solution" name. At least, some specialist may known more about it and you can ask him.
type InactiveAccount struct{}
